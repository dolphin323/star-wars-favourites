import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    borderRadius: 6,
    backgroundColor: "red",
    marginTop: 32,
    paddingVertical: 10,
    paddingHorizontal: 15,
  },
  title: {
    color: "white",
    fontSize: 20,
    fontWeight: "600",
    textAlign: "center",
    width: "100%",
  },
});

export { styles };
